# yaml-language-server: $schema=https://api.cerbos.dev/latest/cerbos/policy/v1/Policy.schema.json
---
apiVersion: api.cerbos.dev/v1
description: Derived roles for common
derivedRoles:
  name: common
  definitions:
    - name: SALES_EMEA
      parentRoles:
        - SALES
      condition:
        match:
          expr: P.attr.region=="EMEA"
